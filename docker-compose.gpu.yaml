services:
  embeddings:
    build:
      dockerfile: Dockerfile.gpu
      args:
        PYTORCH_TAG: ${PYTORCH_TAG:?PYTORCH_TAG is required}
        INSTALL_FLASH_ATTN: ${INSTALL_FLASH_ATTN:-0}
    gpus: all
    ipc: host
